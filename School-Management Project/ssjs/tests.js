/*** @author admin*/currentSession().promoteWith(__myNameSpace.ROLES.DirectoryROLES);var index = 0;function nextPerson(){    var    city	= 'City ' + index,    ln		= 'lastname' + index,    fn 		= 'firstname' + index,    res 	= {},    randomIndex;	    res 	= {        login 		: fn + '.' + ln,        password	: "wakanda",        email		: fn.toLowerCase() + '.' + ln.toLowerCase() + '@wakanda.org',        address		: city,        firstname	: ln,        lastname	: fn    };	    return res;}varparent 		= ds.Parent.first(),studyGroups	= ds.StudyGroup.toArray('ID');for(var _j = 0 ; _j < 10 ; _j++){    var    student		= new ds.Student(nextPerson()),    studyGroupID= studyGroups[parseInt(Math.random()*studyGroups.length)],    studyGroup 	= ds.StudyGroup(studyGroupID);	    student.parent 		= parent;    student.lastname	= parent.lastname;    student.studyGroup	= studyGroup;    student.save();}